{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>About!</h1>
                <p>This is the about page</p>

                <p>this came from the handler: {{index .StringMap "test"}}</p>
                
                <p>Cheers</p>
            </div>
        </div>
    </div>
{{end}}
